import { INCREMENT_USER_ID, INCREMENT_GROUP_ID } from './actionTypes'

export const incrementUserId = () => ({
    type: INCREMENT_USER_ID
})

export const incrementGroupId = () => ({
    type: INCREMENT_GROUP_ID
})

